import { MedColor } from '../../../../interface';
export declare class MedSubtitle {
  /**
    * Define a cor do componente.
    */
  dsColor?: MedColor;
  /**
  * Define a variação do componente.
  */
  dsName?: 'high';
  /**
   * Define a variação de tamanho do componente.
   */
  dsSize?: 'xxs' | 'xs' | 'sm' | 'base' | 'md' | 'lg' | 'xl';
  render(): any;
}
